%div.left-content-group-list
  - current_user.groups.each do |group|
    = link_to group_messages_path(group) do
      %div.left-content-group-list__name=group.name
      %div.left-content-group-list__latest-message 新着メッセージ
